# Reusable MLOps Orchestration Layer

Reusable MLOps orchestration layer for running, evaluating, comparing, and promoting models across multiple ML systems using a shared adapter contract and deterministic quality gates.

## Current status

Bootstrap phase:
- project packaging
- CLI skeleton
- Makefile
- test scaffold

## Quick start

```bash
pip install -r requirements.txt
mlops list
make test